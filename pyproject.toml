[project]
name = "hyper-alpha-arena"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "loguru>=0.7.3",
    "openai>=2.6.1",
    "pyyaml>=6.0.3",
    "sqlalchemy>=2.0.44",
]
